---
"waypoint-codex": patch
---

Make `conversation-retrospective` a default closeout step after major work pieces and strengthen the shipped skill so it explicitly preserves user feedback, captures concrete errors and friction, and evaluates whether used skills succeeded, partially succeeded, or failed.
